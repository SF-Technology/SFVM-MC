
一、新增功能
前端：
1、vm页面展示去除UUID项,增加应用管理信息
2、应用管理员首页新增查看运行中与停止中的VM信息
3、 HOST页面增加镜像同步功能
后端：无

二、BUG
1、修复迁移页面ESX迁移功能中的联动带出重复信息bug
2、虚拟机创建：镜像下载网速计算过滤掉性能数据中空值情况
3、修复虚拟机冷迁移后没有修改虚拟机状态为关机

三、优化
前端：无
后端：
1、vm展示页面后台添加app_info信息返回，更新displayname返回
2、v2v相关功能及镜像同步相关功能统一系统账户调用方式
3、虚拟机创建：物理机筛选算法优化
4、虚拟机状态获取函数，把不需要更新的状态（如：克隆中、冷迁移中、创建中、创建失败）放到一个列表中，状态获取进程不更新处于这些状态的虚拟机。


