一、新增功能
前端：
1、新增虚拟机配置页面磁盘扩容界面
后端：
1、新增虚拟机磁盘扩容功能，用户可以对linux mount点扩容以及给windows新增硬盘
2、修改ip分配逻辑，新增虚拟机创建ip预分配接口

二、BUG
1、修复物理机资源接口返回指定环境、机房、网络环境可用ip数不准确的bug

三、优化
前端：无
后端：
1、物理机性能数据采集CPU判断逻辑优化
2、新增返回总部信息、地区信息返回的信息
3、优化虚拟机创建逻辑适应生产上线
4、物理机性能数据采集CPU判断逻辑优化


数据库改动
1、数据库instance表status的枚举值需要增加‘104’
2、新增segment_match表格用于存放生产、容灾ip对应关系
3、ip表格新增预分配状态
