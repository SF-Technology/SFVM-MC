ID: 20170619

一、新增功能
1、	VM克隆后删除网卡配置，防止启动ip冲突
2、	VM在线连接、断开网卡（暂仅支持Linux）

二、修复BUG
1、v2v模块openstack平台迁移，VM页面看不到被迁移VM：修复之前查找host信息会找到被删除的host信息
2、VM Console页面有时打不开：HOST查询Sql查到多台HOST时，导致HOST错误

三、优化
1、VM配置页面错误流提示
