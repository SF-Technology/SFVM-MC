优化：
1、新增libvirt连接判断逻辑，libvirt连接失败会尝试3次。使用libvirt连接的函数需判断libvirt连接是否为none，none代表libvirt连接失败，需要更新数据库步骤状态为失败
2、更新物理机初始化libvirt.conf配置文件内容
7、修改虚拟机创建锁判断逻辑，因为程序异常退出情况下线释放锁
8、创建物理机添加当libvirt连接3次失败后的错误处理
BUG：
3、修改VM console代码用户认证部分，用户认证地址指向localhost:8080端口
4、修改前端host console页面地址获取方式为截取本页面地址加端口4200访问后端

5、修改数据库instance表新增克隆失败状态101，已经更新到sit、dev、stg生产表
6、克隆虚拟机代码中克隆新增刷新池功能，修复因为虚拟机迁移导致其他关于本台虚拟机操作时找不到存储卷报错

